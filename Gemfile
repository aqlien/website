source 'https://rubygems.org'

gem 'coffee-rails', '~> 4.0.0'
gem "foundation-rails"
gem 'jbuilder', '~> 2.0'
gem 'jquery-rails'
gem "minitest-rails"
gem 'rails'
gem 'sass-rails', '~> 4.0.3'
gem 'sdoc', '~> 0.4.0',          group: :doc
gem 'sqlite3', group: :development, group: :test
gem 'uglifier', '>= 1.3.0'

group :development do
  gem 'spring'
end

group :test do
  gem "launchy"
  gem "minitest-rails-capybara"
end

group :production do
  gem "pg"
  gem "rails_12factor"
end

# Make sure turbolinks is compatible with other options before using it
#gem 'turbolinks'
