<%= form_for([@commentable, @commentable.comments.build]) do |c| %>
<%= c.label :author, "Name: " %>
  <%= c.text_field :author %>
  <%= c.label :content, "Comment Text: " %>
  <%= c.text_area :content%>
  <%= c.submit("Submit comment") %>
<% end %>
